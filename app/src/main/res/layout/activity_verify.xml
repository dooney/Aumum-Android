<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/am_red">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_centerInParent="true"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:gravity="center"
            android:paddingLeft="@dimen/margin_extreme_large"
            android:paddingRight="@dimen/margin_extreme_large">

            <TextView
                android:id="@+id/text_label_phone"
                style="@style/TextView.Account"
                android:paddingBottom="@dimen/margin_medium"/>

            <TextView
                android:id="@+id/text_label_code_tip"
                style="@style/TextView.Account"
                android:paddingBottom="@dimen/margin_large"/>

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/list_edit_text"
                android:layout_marginBottom="@dimen/margin_large">

                <EditText
                    android:id="@+id/et_verification_code"
                    style="@style/EditText.VerificationCode"
                    android:imeOptions="actionDone"/>

            </RelativeLayout>

            <Button
                android:id="@+id/b_confirm"
                style="@style/Button.VerifyCode"
                android:layout_marginBottom="@dimen/margin_large"/>

            <Button
                android:id="@+id/b_resend"
                style="@style/Button.ResendCode"/>

        </LinearLayout>

    </ScrollView>

</RelativeLayout>
